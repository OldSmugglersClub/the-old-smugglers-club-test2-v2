# Testprotokoll 4.9.2-HF12-HF54-TEST66

## Automatische Prüfungen

- JavaScript-Syntax von `wettbewerb.js`: bestanden
- JSON-Syntax von `dynamo-dresden.json`: bestanden
- Beide Bilddateien vorhanden und lesbar: bestanden
- OpenLigaDB-Saisonspielplan `bl2/2026` erreichbar: bestanden
- Dynamo-Team-ID 177 in den Tordaten eindeutig filterbar: bestanden
- Aktueller Kontrollwert: J. Sterner, 2 Tore
- Exakt drei Dynamo-Infokarten: durch bestehende Konfiguration bestätigt
- Einheitliche Kartenhöhe 290 Pixel: CSS-Prüfung bestanden
- Überschriften und Ergebniszeilen ohne Umbruch: CSS-Prüfung bestanden
- Andere Wettbewerbskacheln: keine fachliche Logik geändert

## Sichtprüfung auf Test2v2

Nach dem Einspielen bitte insbesondere Logoausschnitt, Textgröße und gemeinsame Führende auf dem tatsächlichen Zielgerät prüfen. Erst nach `passt` darf ein Live-Release abgeleitet werden.
