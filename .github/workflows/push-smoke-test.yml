name: Push Smoke Test

on:
  push:
    branches:
      - main
    paths:
      - '.github/push-smoke-trigger.txt'

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Push trigger test
        run: echo "push-smoke-test"
